extends ../layouts/main-layout.pug

block styles
  link(rel="stylesheet", href="/css/product.css")

block content
  main.centered
    h1 #{ product.title }
    hr
    .image
      img(src=product.imageUrl, alt=product.title)
    h2 $#{ product.price }
    p #{ product.description }
    include ../layouts/add-to-cart-form.pug
