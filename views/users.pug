extends layouts/main-layout.pug

block content
    main
        if users.length > 0
            ul
                each user in users
                    li #{user.name}
        else
            h1 No Users